services:
  - type: web
    name: motion-backend
    env: docker
    dockerContext: backend
    dockerfilePath: backend/Dockerfile
    plan: starter
    region: ohio
    envVars:
      - key: PORT
        value: 8000
