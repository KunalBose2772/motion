services:
  - type: web
    name: motion-backend
    env: docker
    dockerContext: backend
    dockerfilePath: backend/Dockerfile
    plan: starter
    region: ohio
    envVars:
      - key: PORT
        value: 8000
      - key: YOLO_MODEL
        value: yolov8s-worldv2.pt

